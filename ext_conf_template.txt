# cat=api/enable; type=string; label=API base path: Path prefix of the protocol endpoints (A2A JSON-RPC and HTTP+JSON, AG-UI, A2UI, UCP REST, AP2). Change it only if a site already uses the default for its own pages.
apiBasePath = /api/agent-nexus

# cat=api/enable; type=boolean; label=Publish well-known documents: Serve /.well-known/agent-card.json (A2A) and /.well-known/ucp (UCP) on every host of this installation. Turn it off if another extension publishes them.
publishWellKnown = 1

# cat=traffic/enable; type=boolean; label=Record the traffic log: Write one row per protocol exchange (request, response, streamed events) for the backend traffic log.
trafficEnabled = 1

# cat=traffic/enable; type=int+; label=Traffic retention (days): The agentnexus:cleanup command deletes traffic entries older than this. 0 keeps them until they are deleted by hand.
trafficRetentionDays = 14

# cat=traffic/enable; type=boolean; label=Record request and response bodies: When off, the traffic log keeps only method, path, status, timing and headers.
trafficCaptureBodies = 1

# cat=traffic/enable; type=boolean; label=Mask personal data: Replace names, email addresses, phone numbers and addresses in recorded bodies before they are stored.
trafficRedactPersonalData = 1

# cat=traffic/enable; type=int+; label=Protocol object retention (days): The agentnexus:cleanup command deletes tasks, runs, checkout sessions and mandates not changed for this long. 0 keeps them.
objectRetentionDays = 90

# cat=llm/enable; type=boolean; label=Enable frontend LLM calls: Global switch for all real LLM usage from the frontend plugins. When off, every plugin falls back to its deterministic scripted demo.
llmFrontendEnabled = 1

# cat=llm/enable; type=string; label=Daily LLM budget (USD): Frontend LLM calls stop (deterministic fallback) once today's combined Agent Nexus spend reaches this amount. 0 disables the cap.
llmDailyBudget = 2.00

# cat=llm/enable; type=int+; label=Fallback output tokens per frontend call: The output budget of every protocol whose own budget (a2uiLlmMaxOutputTokens, aguiLlmMaxOutputTokens, ...) is 0. Plugin settings may lower a budget but never exceed it.
llmMaxOutputTokens = 700

# cat=a2ui/enable; type=boolean; label=Enable A2UI LLM generation: When on and netresearch/nr-llm is installed with a provider, A2UI uses a model. When off, the deterministic generator is used.
a2uiLlmEnabled = 1

# cat=a2ui/model; type=string; label=A2UI LLM model: Optional model id passed to nr-llm. Leave empty to use the provider default.
a2uiLlmModel =

# cat=a2ui/enable; type=int+; label=A2UI output tokens per model call: A generated form is a JSON document of about 500 to 900 output tokens (reasoning included); longer forms and other languages need more. An answer cut off at this limit is replaced by the built-in generator, and the provenance says why. 0 uses llmMaxOutputTokens.
a2uiLlmMaxOutputTokens = 1600

# cat=agui/enable; type=boolean; label=Enable AG-UI LLM answers: When on, the frontend assistant streams a real model answer. The approval gate and lead write stay scripted either way.
aguiLlmEnabled = 1

# cat=agui/enable; type=int+; label=AG-UI output tokens per answer: Upper bound for one streamed answer of the site assistant; the element's own "Max answer tokens" may lower it. 0 uses llmMaxOutputTokens.
aguiLlmMaxOutputTokens = 700

# cat=agui/apply; type=boolean; label=AG-UI really apply approved writes: Keep off for demos. When off, approved writes are simulated and logged only.
aguiReallyApply = 0

# cat=a2a/enable; type=boolean; label=Enable A2A LLM routing: When on, skill routing (and the routing rationale) come from a real model; malformed answers fall back to keyword routing.
a2aLlmEnabled = 1

# cat=a2a/enable; type=int+; label=A2A output tokens per model call: Budget for the deliverable a skill writes; the routing answer uses at most 160 of it. 0 uses llmMaxOutputTokens.
a2aLlmMaxOutputTokens = 400

# cat=ucp/enable; type=boolean; label=Enable UCP LLM rationale: When on, the shopping agent's recommendation text is model-written. Cart contents and prices always stay deterministic.
ucpLlmEnabled = 1

# cat=ucp/enable; type=int+; label=UCP output tokens per rationale: Two sentences need well under 100 tokens. 0 uses llmMaxOutputTokens.
ucpLlmMaxOutputTokens = 160

# cat=ucp/links; type=string; label=UCP terms of service URL: An absolute https URL. Every checkout session links to it as its terms_of_service; leave empty to omit the link.
ucpTermsOfServiceUrl =

# cat=ucp/links; type=string; label=UCP privacy policy URL: An absolute https URL. Every checkout session links to it as its privacy_policy; leave empty to omit the link.
ucpPrivacyPolicyUrl =

# cat=ucp/apply; type=boolean; label=UCP really charge: Keep off for demos. When off, every checkout is simulated and no order is placed with a real system.
ucpReallyApply = 0

# cat=ap2/enable; type=boolean; label=Enable AP2 LLM explainer: When on, receipts include a model-written plain-language explanation of the verified mandate chain. The mandates themselves are always deterministic.
ap2LlmEnabled = 0

# cat=ap2/enable; type=int+; label=AP2 output tokens per explanation: Two sentences need well under 100 tokens. 0 uses llmMaxOutputTokens.
ap2LlmMaxOutputTokens = 160
