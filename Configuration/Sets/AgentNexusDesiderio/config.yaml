# Kept for compatibility only. Deprecated since 3.1: it does nothing, because
# the widgets take their look from the host theme's shadcn tokens
# (Resources/Public/Css/nexus-tokens.css). Sites that list it keep building -
# TYPO3 refuses to build a site whose dependencies name an unknown set.
# Removal is planned for 5.0: depend on "webconsulting/agent-nexus" instead.
name: webconsulting/agent-nexus-desiderio
label: 'Agent Nexus: Desiderio rendering (deprecated, does nothing since 3.1)'
dependencies:
  - webconsulting/agent-nexus
